spring:
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  flyway:
    baseline-on-migrate: true
    validate-on-migrate: false
server:
  port: 8081
  compression:
    enabled: true
fastdfs:
  nginx:
    host: http://47.111.82.18/